/**
 * Classes that control the robot's mechanisms. Most of these are mounted to an actuating
 * {@link frc.robot.subsystems.mechanisms.arm.Pivot Pivot} and
 * {@link frc.robot.subsystems.mechanisms.arm.Elevator Elevator} (grouped into the
 * {@link frc.robot.subsystems.mechanisms.arm.Arm Arm} subsystem).
 */
package frc.robot.subsystems.mechanisms;
